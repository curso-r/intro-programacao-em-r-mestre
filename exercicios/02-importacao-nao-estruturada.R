# O objetivo desse exercício é construir um script em R para ler a base imdb_nao_estruturada.xlsx. 

# Essa base não está bem estruturada, então você não conseguir importá-la para o R apenas passando o 
# argumento path do read_excel(). Assim como vimos em aula, utilize os argumentos da função read_excel() 
# para importar esse arquivo sem modificá-lo diretamente pelo excel.

# Importante: repare que, ao contrário da base IMDB que vimos em aula, essa não tem nomes na colunas. 
# Portanto, os nomes gerados na importação para as colunas serão genéricos (por exemplo, V1, V2, V3, ...).

# Você deve entregar apenas o script em R, não é preciso fazer o upload  do arquivo Excel.